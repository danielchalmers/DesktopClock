name: Check Formatting

description: Restores dependencies and verifies formatting.

runs:
  using: "composite"
  steps:
    - uses: actions/setup-dotnet@v5
      with:
        global-json-file: global.json

    - name: Restore
      shell: bash
      run: dotnet restore

    - name: Check Formatting
      shell: bash
      run: dotnet format --verify-no-changes --no-restore
