<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net481</TargetFramework>
    <UseWPF>true</UseWPF>
    <UseWindowsForms>true</UseWindowsForms>
    <LangVersion>latest</LangVersion>
    <ApplicationIcon>DesktopClock.ico</ApplicationIcon>
  </PropertyGroup>

  <PropertyGroup>
    <Company>Daniel Chalmers</Company>
    <Description>A digital clock for your desktop that's ready whenever you need it.</Description>
    <Copyright>© Daniel Chalmers</Copyright>
    <IncludeSourceRevisionInInformationalVersion>false</IncludeSourceRevisionInInformationalVersion>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="CommunityToolkit.Mvvm" Version="8.4.1" />
    <PackageReference Include="Costura.Fody" Version="6.0.0" PrivateAssets="All" />
    <PackageReference Include="H.NotifyIcon.Wpf" Version="2.4.1" />
    <PackageReference Include="Newtonsoft.Json" Version="13.0.4" />
    <PackageReference Include="PropertyChanged.Fody" Version="4.1.0" PrivateAssets="All" />
    <PackageReference Include="WpfWindowPlacement" Version="6.0.0" />
  </ItemGroup>

  <ItemGroup>
    <Resource Include="DesktopClock.ico" />
  </ItemGroup>

  <ItemGroup>
    <!-- Translations compile into the exe instead of satellite assemblies, which Costura doesn't embed, so the app stays a single file. See Loc. -->
    <EmbeddedResource Update="Localization\Strings.*.resx" WithCulture="false" />
  </ItemGroup>
</Project>
