<Window x:Class="DesktopClock.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesktopClock"
        xmlns:p="clr-namespace:DesktopClock.Properties"
        d:DataContext="{d:DesignInstance Type=local:SettingsWindowViewModel}"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        mc:Ignorable="d"
        Title="DesktopClock"
        Width="600"
        ResizeMode="CanMinimize"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="MinHeight" Value="28" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Margin" Value="0,0,0,4" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="MinHeight" Value="26" />
            <Setter Property="Padding" Value="2" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Margin" Value="0,0,0,4" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="MinHeight" Value="26" />
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="BorderBrush" Value="#D0D0D0" />
            <Setter Property="Margin" Value="0,0,0,4" />
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="0,0,0,4" />
        </Style>

        <Style TargetType="Slider">
            <Setter Property="Margin" Value="0,0,0,4" />
        </Style>

        <Style x:Key="LabelTextBlock"
               TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,0,0,4" />
        </Style>

        <Style x:Key="DescriptionTextBlock"
               TargetType="TextBlock">
            <Setter Property="FontStyle" Value="Italic" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Margin" Value="0,0,0,16" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>

        <Style TargetType="Hyperlink">
            <Setter Property="TextDecorations" Value="None" />
        </Style>

        <Style x:Key="SectionHeader"
               TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>
    </Window.Resources>

    <TabControl Padding="12,12,12,0"
                SelectedIndex="{Binding Settings.SettingsTabIndex, Mode=TwoWay}">
        <TabItem Header="_General">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          Padding="0">
                <StackPanel>
                    <TextBlock Text="Clock Format:"
                               Style="{StaticResource LabelTextBlock}" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="20" />
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0"
                                 Text="{Binding Settings.Format, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="True"
                                 MaxLines="3" />
                        <ComboBox Grid.Column="1"
                                  DisplayMemberPath="Example"
                                  ItemsSource="{x:Static local:DateFormatExample.DefaultExamples}"
                                  SelectionChanged="SelectFormat" />
                    </Grid>
                    <TextBlock Style="{StaticResource DescriptionTextBlock}">
                        <Run Text="Customize how the time is displayed. Use curly braces {} for time codes." />
                        <Hyperlink NavigateUri="https://learn.microsoft.com/en-us/dotnet/standard/base-types/custom-date-and-time-format-strings"
                                   RequestNavigate="Hyperlink_RequestNavigate">
                            Learn more
                        </Hyperlink>
                    </TextBlock>

                    <TextBlock Text="Time Zone:"
                               Style="{StaticResource LabelTextBlock}" />
                    <ComboBox ItemsSource="{Binding TimeZones}"
                              SelectedItem="{Binding Settings.TimeZoneInfo, Mode=TwoWay}"
                              DisplayMemberPath="DisplayName" />
                    <TextBlock Text="Show time from a different time zone."
                               Style="{StaticResource DescriptionTextBlock}" />

                    <CheckBox Content="Start with Windows"
                              IsChecked="{Binding Settings.RunOnStartup, Mode=TwoWay}" />
                    <TextBlock Text="Run the clock automatically when you sign in."
                               Style="{StaticResource DescriptionTextBlock}" />

                    <CheckBox Content="Show in Taskbar"
                              IsChecked="{Binding Settings.ShowInTaskbar, Mode=TwoWay}" />
                    <TextBlock Text="Show the clock in the bottom bar."
                               Style="{StaticResource DescriptionTextBlock}" />

                    <CheckBox Content="Always on Top"
                              IsChecked="{Binding Settings.Topmost, Mode=TwoWay}" />
                    <TextBlock Text="Keep the clock above other windows."
                               Style="{StaticResource DescriptionTextBlock}" />

                    <CheckBox Content="Start Hidden"
                              IsChecked="{Binding Settings.StartHidden, Mode=TwoWay}" />
                    <TextBlock Text="Start without showing the clock. Click the tray icon to show it."
                               Style="{StaticResource DescriptionTextBlock}" />

                    <CheckBox Content="Burn-in Protection"
                              IsChecked="{Binding Settings.BurnInMitigation, Mode=TwoWay}" />
                    <TextBlock Text="Move the clock slightly every so often to protect your screen."
                               Style="{StaticResource DescriptionTextBlock}" />
                </StackPanel>
            </ScrollViewer>
        </TabItem>

        <TabItem Header="_Text">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          Padding="0">
                <StackPanel>
                    <TextBlock Text="Font Family:"
                               Style="{StaticResource LabelTextBlock}" />
                    <ComboBox ItemsSource="{Binding FontFamilies}"
                              SelectedItem="{Binding Settings.FontFamily, Mode=TwoWay}" />
                    <TextBlock Text="Choose the font style."
                               Style="{StaticResource DescriptionTextBlock}" />

                    <TextBlock Text="Font Style:"
                               Style="{StaticResource LabelTextBlock}" />
                    <ComboBox ItemsSource="{Binding FontStyles}"
                              SelectedItem="{Binding Settings.FontStyle, Mode=TwoWay}" />
                    <TextBlock Text="Make text lean to the side."
                               Style="{StaticResource DescriptionTextBlock}" />

                    <TextBlock Text="Font Weight:"
                               Style="{StaticResource LabelTextBlock}" />
                    <ComboBox ItemsSource="{Binding FontWeights}"
                              SelectedItem="{Binding Settings.FontWeight, Mode=TwoWay}" />
                    <TextBlock Text="Make text thicker or thinner."
                               Style="{StaticResource DescriptionTextBlock}" />

                    <TextBlock Text="Capitalization:"
                               Style="{StaticResource LabelTextBlock}" />
                    <ComboBox ItemsSource="{Binding TextTransforms}"
                              SelectedItem="{Binding Settings.TextTransform, Mode=TwoWay}" />
                    <TextBlock Text="Change text to uppercase or lowercase."
                               Style="{StaticResource DescriptionTextBlock}" />

                    <TextBlock Text="Text Color:"
                               Style="{StaticResource LabelTextBlock}" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="4" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBox Text="{Binding Settings.TextColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Grid.Column="0" />
                        <Button Content="Pick..."
                                Click="PickTextColor"
                                Grid.Column="2" />
                    </Grid>
                    <TextBlock Text="Change the color of the text (hex code or name)."
                               Style="{StaticResource DescriptionTextBlock}" />

                    <TextBlock Text="Text Visibility:"
                               Style="{StaticResource LabelTextBlock}" />
                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="30" />
                        </Grid.ColumnDefinitions>
                        <Slider Value="{Binding Settings.TextOpacity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                Minimum="0"
                                Maximum="1"
                                TickFrequency="0.01"
                                IsSnapToTickEnabled="True"
                                Grid.Column="0"
                                Margin="0" />
                        <TextBlock Text="{Binding Settings.TextOpacity, StringFormat=0.00}"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Right"
                                   Grid.Column="1" />
                    </Grid>
                    <TextBlock Text="Adjust how transparent the text is."
                               Style="{StaticResource DescriptionTextBlock}" />
                </StackPanel>
            </ScrollViewer>
        </TabItem>

        <TabItem Header="_Background">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          Padding="0">
                <StackPanel>
                    <TextBlock Text="Background/Outline Color:"
                               Style="{StaticResource LabelTextBlock}" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="4" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBox Text="{Binding Settings.OuterColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Grid.Column="0" />
                        <Button Content="Pick..."
                                Click="PickOuterColor"
                                Grid.Column="2" />
                    </Grid>
                    <TextBlock Text="Change the color of the background or outline."
                               Style="{StaticResource DescriptionTextBlock}" />

                    <CheckBox Content="Solid Background"
                              IsChecked="{Binding Settings.BackgroundEnabled, Mode=TwoWay}" />
                    <TextBlock Text="Show a solid background box instead of an outline."
                               Style="{StaticResource DescriptionTextBlock}" />

                    <TextBlock Text="Background Visibility:"
                               Style="{StaticResource LabelTextBlock}" />
                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="30" />
                        </Grid.ColumnDefinitions>
                        <Slider Value="{Binding Settings.BackgroundOpacity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                Minimum="0"
                                Maximum="1"
                                TickFrequency="0.01"
                                IsSnapToTickEnabled="True"
                                Grid.Column="0"
                                Margin="0" />
                        <TextBlock Text="{Binding Settings.BackgroundOpacity, StringFormat=0.00}"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Right"
                                   Grid.Column="1" />
                    </Grid>
                    <TextBlock Text="Adjust how transparent the background is."
                               Style="{StaticResource DescriptionTextBlock}" />

                    <TextBlock Text="Rounded Corners:"
                               Style="{StaticResource LabelTextBlock}" />
                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="30" />
                        </Grid.ColumnDefinitions>
                        <Slider Value="{Binding Settings.BackgroundCornerRadius, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                Minimum="0"
                                Maximum="24"
                                TickFrequency="0.1"
                                IsSnapToTickEnabled="True"
                                Grid.Column="0"
                                Margin="0" />
                        <TextBlock Text="{Binding Settings.BackgroundCornerRadius, StringFormat=00.0}"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Right"
                                   Grid.Column="1" />
                    </Grid>
                    <TextBlock Text="Soften the background edges."
                               Style="{StaticResource DescriptionTextBlock}" />

                    <TextBlock Text="Outline Thickness:"
                               Style="{StaticResource LabelTextBlock}" />
                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="30" />
                        </Grid.ColumnDefinitions>
                        <Slider Value="{Binding Settings.OutlineThickness, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                Minimum="0"
                                Maximum="5"
                                TickFrequency="0.1"
                                IsSnapToTickEnabled="True"
                                Grid.Column="0"
                                Margin="0" />
                        <TextBlock Text="{Binding Settings.OutlineThickness, StringFormat=0.0}"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Right"
                                   Grid.Column="1" />
                    </Grid>
                    <TextBlock Text="Adjust the outline weight when the background is off."
                               Style="{StaticResource DescriptionTextBlock}" />

                    <TextBlock Text="Background Image:"
                               Style="{StaticResource LabelTextBlock}" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="4" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="4" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBox Text="{Binding Settings.BackgroundImagePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Grid.Column="0" />
                        <Button Content="Browse..."
                                Click="BrowseBackgroundImagePath"
                                Grid.Column="2" />
                        <Button Content="Reset"
                                Command="{Binding ResetBackgroundImagePathCommand}"
                                Grid.Column="4" />
                    </Grid>
                    <TextBlock Text="Show an image behind the clock."
                               Style="{StaticResource DescriptionTextBlock}" />

                    <ComboBox ItemsSource="{Binding ImageStretches}"
                              SelectedItem="{Binding Settings.BackgroundImageStretch, Mode=TwoWay}" />
                    <TextBlock Text="Choose how the image fits within the clock."
                               Style="{StaticResource DescriptionTextBlock}" />
                </StackPanel>
            </ScrollViewer>
        </TabItem>

        <TabItem Header="Countdown &amp; Sounds">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          Padding="0">
                <StackPanel>
                    <TextBlock Text="Countdown Target:"
                               Style="{StaticResource LabelTextBlock}" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="4" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBox Text="{Binding Settings.CountdownTo, Mode=TwoWay}"
                                 Grid.Column="0" />
                        <Button Content="Reset"
                                Command="{Binding ResetCountdownCommand}"
                                Grid.Column="2" />
                    </Grid>
                    <TextBlock Text="Set a date and time to count down to. Clear to stop."
                               Style="{StaticResource DescriptionTextBlock}" />

                    <TextBlock Text="Countdown Format:"
                               Style="{StaticResource LabelTextBlock}" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="4" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBox Text="{Binding Settings.CountdownFormat, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="True"
                                 MaxLines="3"
                                 Grid.Column="0" />
                        <Button Content="Reset"
                                Command="{Binding ResetCountdownFormatCommand}"
                                Grid.Column="2" />
                    </Grid>
                    <TextBlock Style="{StaticResource DescriptionTextBlock}">
                        <Run Text="Customize how the countdown is displayed. Leave empty for default." />
                        <Hyperlink NavigateUri="https://learn.microsoft.com/en-us/dotnet/standard/base-types/custom-timespan-format-strings"
                                   RequestNavigate="Hyperlink_RequestNavigate">
                            Learn more
                        </Hyperlink>
                    </TextBlock>

                    <TextBlock Text="Chime Sound:"
                               Style="{StaticResource LabelTextBlock}" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="4" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="4" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBox Text="{Binding Settings.WavFilePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Grid.Column="0" />
                        <Button Content="Browse..."
                                Click="BrowseWavFilePath"
                                Grid.Column="2" />
                        <Button Content="Reset"
                                Command="{Binding ResetWavFilePathCommand}"
                                Grid.Column="4" />
                    </Grid>
                    <TextBlock Text="Select a sound file (.wav) to play."
                               Style="{StaticResource DescriptionTextBlock}" />

                    <TextBlock Text="Chime Frequency:"
                               Style="{StaticResource LabelTextBlock}" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="4" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBox Text="{Binding Settings.WavFileInterval, Mode=TwoWay}"
                                 Grid.Column="0" />
                        <Button Content="Reset"
                                Command="{Binding ResetWavFileIntervalCommand}"
                                Grid.Column="2" />
                    </Grid>
                    <TextBlock Text="How often to play the sound (Hours:Minutes:Seconds)."
                               Style="{StaticResource DescriptionTextBlock}" />

                    <CheckBox Content="Countdown Alarm"
                              IsChecked="{Binding Settings.PlaySoundOnCountdown, Mode=TwoWay}" />
                    <TextBlock Text="Play the sound when the countdown finishes."
                               Style="{StaticResource DescriptionTextBlock}" />
                </StackPanel>
            </ScrollViewer>
        </TabItem>

        <TabItem Header="_Advanced">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          Padding="0">
                <StackPanel>
                    <CheckBox Content="Allow Dragging"
                              IsChecked="{Binding Settings.DragToMove, Mode=TwoWay}" />
                    <TextBlock Text="Move the clock by dragging it with the mouse."
                               Style="{StaticResource DescriptionTextBlock}" />

                    <CheckBox Content="Click Through"
                              IsChecked="{Binding Settings.ClickThrough, Mode=TwoWay}" />
                    <TextBlock Text="Let clicks pass through the clock to windows behind it."
                               Style="{StaticResource DescriptionTextBlock}" />

                    <CheckBox Content="Align Right"
                              IsChecked="{Binding Settings.RightAligned, Mode=TwoWay}" />
                    <TextBlock Text="Keep the right side fixed when the text length changes."
                               Style="{StaticResource DescriptionTextBlock}" />

                    <Button Content="Open Folder"
                            Click="OpenSettingsFolder"
                            IsEnabled="{x:Static p:Settings.CanBeSaved}" />
                    <TextBlock Text="Open the folder where the settings file is stored."
                               Style="{StaticResource DescriptionTextBlock}" />

                    <Button Content="Open Settings File"
                            Click="OpenSettingsFile"
                            IsEnabled="{x:Static p:Settings.CanBeSaved}" />
                    <TextBlock Text="Open the JSON settings file in Notepad."
                               Style="{StaticResource DescriptionTextBlock}" />

                    <Button Content="Create New Clock"
                            Click="CreateNewClock"
                            IsEnabled="{x:Static p:Settings.CanBeSaved}" />
                    <TextBlock Text="Copy this clock with fresh settings in a new instance."
                               Style="{StaticResource DescriptionTextBlock}" />

                    <Button Content="Check for Updates"
                            Click="CheckForUpdates" />
                    <TextBlock Text="Open the latest releases page in your browser."
                               Style="{StaticResource DescriptionTextBlock}" />
                </StackPanel>
            </ScrollViewer>
        </TabItem>

        <TabItem Header="_Help">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          Padding="0">
                <StackPanel>
                    <TextBlock Style="{StaticResource SectionHeader}">
                        Keyboard Shortcuts
                    </TextBlock>
                    <StackPanel Margin="0,0,0,16">
                        <StackPanel.Resources>
                            <Style TargetType="TextBlock">
                                <Setter Property="Margin" Value="0,0,0,4" />
                            </Style>
                        </StackPanel.Resources>
                        <TextBlock>
                            <Run FontWeight="SemiBold">
                                Right-click:
                            </Run>
                            <Run>Open menu</Run>
                        </TextBlock>
                        <TextBlock>
                            <Run FontWeight="SemiBold">
                                Double-click:
                            </Run>
                            <Run>Copy text</Run>
                        </TextBlock>
                        <TextBlock>
                            <Run FontWeight="SemiBold">
                                Drag:
                            </Run>
                            <Run>Move clock</Run>
                        </TextBlock>
                        <TextBlock>
                            <Run FontWeight="SemiBold">
                                Ctrl + Scroll:
                            </Run>
                            <Run>Change size</Run>
                        </TextBlock>
                        <TextBlock>
                            <Run FontWeight="SemiBold">
                                Ctrl + +:
                            </Run>
                            <Run>Make bigger</Run>
                        </TextBlock>
                        <TextBlock>
                            <Run FontWeight="SemiBold">
                                Ctrl + -:
                            </Run>
                            <Run>Make smaller</Run>
                        </TextBlock>
                    </StackPanel>

                    <TextBlock Style="{StaticResource SectionHeader}">
                        Links
                    </TextBlock>
                    <StackPanel Margin="0,0,0,16">
                        <StackPanel.Resources>
                            <Style TargetType="TextBlock">
                                <Setter Property="Margin" Value="0,0,0,4" />
                            </Style>
                        </StackPanel.Resources>
                        <TextBlock>
                            <Hyperlink NavigateUri="https://github.com/danielchalmers/DesktopClock/releases"
                                       RequestNavigate="Hyperlink_RequestNavigate">
                                GitHub Releases (Check for updates)
                            </Hyperlink>
                        </TextBlock>
                        <TextBlock>
                            <Hyperlink NavigateUri="https://github.com/danielchalmers/DesktopClock/issues"
                                       RequestNavigate="Hyperlink_RequestNavigate">
                                GitHub Issues (Found a bug or have an idea?)
                            </Hyperlink>
                        </TextBlock>
                        <TextBlock>
                            <Hyperlink NavigateUri="https://play.google.com/store/apps/details?id=com.danielchalmers.journalapp"
                                       RequestNavigate="Hyperlink_RequestNavigate">
                                Good Diary - Stay on top of your well-being (Android)
                            </Hyperlink>
                        </TextBlock>
                        <TextBlock>
                            <Hyperlink NavigateUri="https://github.com/danielchalmers/Network-Monitor"
                                       RequestNavigate="Hyperlink_RequestNavigate">
                                Network Monitor - See latency and bandwidth usage (Windows)
                            </Hyperlink>
                        </TextBlock>
                        <TextBlock>
                            <Hyperlink NavigateUri="https://github.com/danielchalmers/SentryReplay"
                                       RequestNavigate="Hyperlink_RequestNavigate">
                                Sentry Replay - Watch your Tesla dashcam clips (Windows)
                            </Hyperlink>
                        </TextBlock>
                        <TextBlock>
                            <Hyperlink NavigateUri="https://github.com/danielchalmers/RadialActions"
                                       RequestNavigate="Hyperlink_RequestNavigate">
                                Radial Actions - Summon a customizable radial menu (Windows)
                            </Hyperlink>
                        </TextBlock>
                        <TextBlock>
                            <Hyperlink NavigateUri="https://github.com/danielchalmers/Teichos"
                                       RequestNavigate="Hyperlink_RequestNavigate">
                                Teichos - Focus better by blocking distracting websites (Browser)
                            </Hyperlink>
                        </TextBlock>
                        <TextBlock>
                            <Hyperlink NavigateUri="https://github.com/danielchalmers"
                                       RequestNavigate="Hyperlink_RequestNavigate">
                                All my projects on GitHub
                            </Hyperlink>
                        </TextBlock>
                    </StackPanel>

                    <TextBlock Style="{StaticResource SectionHeader}">
                        Credits
                    </TextBlock>
                    <StackPanel Margin="0,0,0,16">
                        <StackPanel.Resources>
                            <Style TargetType="TextBlock">
                                <Setter Property="Margin" Value="0,0,0,4" />
                            </Style>
                        </StackPanel.Resources>
                        <TextBlock>
                            <Hyperlink NavigateUri="https://github.com/CommunityToolkit/dotnet"
                                       RequestNavigate="Hyperlink_RequestNavigate">
                                CommunityToolkit
                            </Hyperlink>
                        </TextBlock>
                        <TextBlock>
                            <Hyperlink NavigateUri="https://github.com/Fody/Costura"
                                       RequestNavigate="Hyperlink_RequestNavigate">
                                Costura.Fody
                            </Hyperlink>
                        </TextBlock>
                        <TextBlock>
                            <Hyperlink NavigateUri="https://github.com/HavenDV/H.NotifyIcon"
                                       RequestNavigate="Hyperlink_RequestNavigate">
                                H.NotifyIcon.Wpf
                            </Hyperlink>
                        </TextBlock>
                        <TextBlock>
                            <Hyperlink NavigateUri="https://github.com/Humanizr/Humanizer"
                                       RequestNavigate="Hyperlink_RequestNavigate">
                                Humanizer
                            </Hyperlink>
                        </TextBlock>
                        <TextBlock>
                            <Hyperlink NavigateUri="https://www.newtonsoft.com/json"
                                       RequestNavigate="Hyperlink_RequestNavigate">
                                Newtonsoft.Json
                            </Hyperlink>
                        </TextBlock>
                        <TextBlock>
                            <Hyperlink NavigateUri="https://github.com/Fody/PropertyChanged"
                                       RequestNavigate="Hyperlink_RequestNavigate">
                                PropertyChanged.Fody
                            </Hyperlink>
                        </TextBlock>
                        <TextBlock>
                            <Hyperlink NavigateUri="https://github.com/danielchalmers/WpfWindowPlacement"
                                       RequestNavigate="Hyperlink_RequestNavigate">
                                WpfWindowPlacement
                            </Hyperlink>
                        </TextBlock>
                        <TextBlock>
                            <Hyperlink NavigateUri="https://icon.kitchen/i/H4sIAAAAAAAAA0WP2grCMAyG3yXe7sIDCO5WH0DQOxHJ2rQrdstsOw%2BI725aB%2FaiCV8O%2F5833NGPFKF%2Bg8ZwPbbUEdQGfaQKjN16N2BIuRxJAmgyOPoEFTjFvQBUimK8JCdznzLCnoMUZqY86Wz%2BbLnYrM2qsEOLg0hBvI0uKE8Cjd2j1q63WS%2FxAPViXkFwthXpnDacEne%2F3JMptIgeX2WXmuwWRM9seLctalODDagd9Slb7ViPPp9%2BAux1YKfzVRzlf1AD588XVKijiB0BAAA%3D"
                                       RequestNavigate="Hyperlink_RequestNavigate">
                                IconKitchen
                            </Hyperlink>
                        </TextBlock>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </TabItem>
    </TabControl>
</Window>
