<Window x:Class="DesktopClock.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesktopClock"
        xmlns:p="clr-namespace:DesktopClock.Properties"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        d:DataContext="{d:DesignInstance Type=local:MainWindow}"
        mc:Ignorable="d"
        Title="DesktopClock"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStyle="None"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Opacity="0"
        Topmost="{Binding Topmost, Source={x:Static p:Settings.Default}, Mode=TwoWay}"
        FontFamily="{Binding FontFamily, Source={x:Static p:Settings.Default}, Mode=OneWay}"
        FontStyle="{Binding FontStyle, Source={x:Static p:Settings.Default}, Mode=OneWay}"
        FontWeight="{Binding FontWeight, Source={x:Static p:Settings.Default}, Mode=OneWay}"
        MouseDown="Window_MouseDown"
        MouseDoubleClick="Window_MouseDoubleClick"
        MouseWheel="Window_MouseWheel"
        SourceInitialized="Window_SourceInitialized"
        ContentRendered="Window_ContentRendered"
        Closing="Window_Closing"
        StateChanged="Window_StateChanged"
        KeyDown="Window_KeyDown">
    <Window.Resources>
        <ContextMenu x:Key="MainContextMenu"
                     x:Shared="False">
            <MenuItem Header="{x:Static local:App.MainFileDisplayText}"
                      IsEnabled="False" />

            <Separator />

            <MenuItem Command="{Binding CopyToClipboardCommand}"
                      Header="_Copy">
                <MenuItem.Icon>
                    <TextBlock Text="&#xE8C8;"
                               Style="{StaticResource MenuIconGlyph}" />
                </MenuItem.Icon>
            </MenuItem>

            <MenuItem Command="{Binding HideForNowCommand}"
                      Header="_Hide">
                <MenuItem.Icon>
                    <TextBlock Text="&#xE890;"
                               Style="{StaticResource MenuIconGlyph}" />
                </MenuItem.Icon>
            </MenuItem>

            <MenuItem>
                <MenuItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Size: " />

                        <Slider Width="80"
                                Maximum="{x:Static local:HeightScaleConverter.MaxSizeLog}"
                                Minimum="{x:Static local:HeightScaleConverter.MinSizeLog}"
                                Value="{Binding Height, Converter={local:HeightScaleConverter}, Source={x:Static p:Settings.Default}, Mode=TwoWay}" />
                    </StackPanel>
                </MenuItem.Header>
            </MenuItem>

            <MenuItem Header="Stay on _top"
                      IsCheckable="True"
                      IsChecked="{Binding Topmost, Source={x:Static p:Settings.Default}, Mode=TwoWay}" />

            <MenuItem Command="{Binding OpenSettingsWindowCommand}"
                      Header="_Settings…">
                <MenuItem.Icon>
                    <TextBlock Text="&#xE713;"
                               Style="{StaticResource MenuIconGlyph}" />
                </MenuItem.Icon>
            </MenuItem>

            <Separator />

            <MenuItem Command="{Binding OpenReleasesPageCommand}"
                      Header="Check for _updates">
                <MenuItem.Icon>
                    <TextBlock Text="&#xE895;"
                               Style="{StaticResource MenuIconGlyph}" />
                </MenuItem.Icon>
            </MenuItem>

            <MenuItem Command="{Binding OpenIssueTrackerCommand}"
                      Header="Give _feedback">
                <MenuItem.Icon>
                    <TextBlock Text="&#xED15;"
                               Style="{StaticResource MenuIconGlyph}" />
                </MenuItem.Icon>
            </MenuItem>

            <MenuItem Command="{Binding ExitCommand}"
                      Header="E_xit">
                <MenuItem.Icon>
                    <TextBlock Text="&#xE7E8;"
                               Style="{StaticResource MenuIconGlyph}" />
                </MenuItem.Icon>
            </MenuItem>
        </ContextMenu>

        <tb:TaskbarIcon x:Key="TrayIcon"
                        x:Shared="False"
                        IconSource="DesktopClock.ico"
                        ToolTipText="{x:Static local:App.MainFileDisplayText}"
                        MenuActivation="LeftOrRightClick" />
    </Window.Resources>

    <Viewbox Height="{Binding Height, Source={x:Static p:Settings.Default}, Mode=OneWay}">
        <Border CornerRadius="{Binding BackgroundCornerRadius, Source={x:Static p:Settings.Default}, Mode=OneWay}"
                Padding="1,0,1,0">
            <Border.Background>
                <!-- Falls back to the solid outer color when the background image is missing or unreadable, so the clock is never left with an invisible background. -->
                <MultiBinding Converter="{local:BackgroundBrushConverter}">
                    <Binding Path="BackgroundEnabled" Source="{x:Static p:Settings.Default}" Mode="OneWay" />
                    <Binding Path="BackgroundImagePath" Source="{x:Static p:Settings.Default}" Mode="OneWay" />
                    <Binding Path="OuterColor" Source="{x:Static p:Settings.Default}" Mode="OneWay" />
                    <Binding Path="BackgroundOpacity" Source="{x:Static p:Settings.Default}" Mode="OneWay" />
                    <Binding Path="BackgroundImageStretch" Source="{x:Static p:Settings.Default}" Mode="OneWay" />
                </MultiBinding>
            </Border.Background>

            <local:OutlinedTextBlock Text="{Binding CurrentTimeOrCountdownString}"
                                     StrokeThickness="{Binding OutlineThickness, Source={x:Static p:Settings.Default}, Mode=OneWay}"
                                     TextWrapping="Wrap">

                <local:OutlinedTextBlock.Fill>
                    <SolidColorBrush Opacity="{Binding TextOpacity, Source={x:Static p:Settings.Default}, Mode=OneWay}"
                                     Color="{Binding TextColor, Source={x:Static p:Settings.Default}, Mode=OneWay}" />
                </local:OutlinedTextBlock.Fill>

                <local:OutlinedTextBlock.Style>
                    <Style TargetType="local:OutlinedTextBlock">
                        <Setter Property="Stroke" Value="Transparent" />
                        <Setter Property="Margin" Value="0" />

                        <Style.Triggers>
                            <DataTrigger Binding="{Binding BackgroundEnabled, Source={x:Static p:Settings.Default}, Mode=OneWay}"
                                         Value="False">
                                <DataTrigger.Setters>
                                    <Setter Property="Stroke">
                                        <Setter.Value>
                                            <SolidColorBrush Opacity="{Binding BackgroundOpacity, Source={x:Static p:Settings.Default}, Mode=OneWay}"
                                                             Color="{Binding OuterColor, Source={x:Static p:Settings.Default}, Mode=OneWay}" />
                                        </Setter.Value>
                                    </Setter>

                                    <Setter Property="Margin" Value="{Binding OutlineThickness, Source={x:Static p:Settings.Default}, Mode=OneWay}" />
                                </DataTrigger.Setters>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </local:OutlinedTextBlock.Style>
            </local:OutlinedTextBlock>
        </Border>
    </Viewbox>
</Window>
